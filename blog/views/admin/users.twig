{% extend "layout.twig" %}

{% block content %}

    <h2>Users</h2>
    <a href="{{ 'admin/users/create' | url }}" class="btn btn-primary">New users</a>
    <table class="table">
        <caption>Table content blogs</caption>
        <thead>
        <tr>
            <th>Name</th>
            <th>User</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        </thead>
        <tbody>
        {% for blogPost in blogPosts %}
        <tr>
            <td>{{   blogPost.title }}</td>
            <td>Edit</td>
            <td>Delete</td>
        </tr>
        </tbody>
        {% endfor %}
    </table>

{% endblock %}